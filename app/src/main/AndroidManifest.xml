<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.wadektech.mtihanirevise">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/logoicon"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Adapter.MainSliderActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".Auth.LoginActivity" />
        <activity android:name=".UI.PastPapersActivity" />
        <activity android:name=".UI.PaperPerSubject" />
        <activity android:name=".PdfViewer.PdfViewerActivity" />
        <activity android:name=".UI.DeveloperProfile"></activity>
    </application>

</manifest>